---
// Slim header for single-purpose campaign landing pages: logo + click-to-call only.
// No nav links, no dropdowns — nothing to exit through except the phone number.
import LogoMark from './LogoMark.astro';
---

<header class="min-header">
  <div class="min-header-inner">
    <a href="/" class="min-logo" aria-label="Stronghold Construction — home">
      <LogoMark height={28} />
      <span class="min-logo-wordmark">
        <span class="min-logo-name">Stronghold</span>
        <span class="min-logo-suffix">Construction</span>
      </span>
    </a>
    <a href="tel:+19289627535" class="min-header-phone">
      <span class="min-header-phone-label">Call Now</span>
      <span class="min-header-phone-number">(928) 962-7535</span>
    </a>
  </div>
</header>

<style>
  .min-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background-color: var(--color-dark);
    border-bottom: 2px solid #1c1c1c;
  }

  .min-header-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .min-logo {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    text-decoration: none;
    flex-shrink: 0;
    /* Astro scoped styles don't reach a child component's root node, so the
       mark reads its mountain colour off `currentColor` set here. */
    color: var(--color-brand-white);
  }

  .min-logo-wordmark {
    display: flex;
    flex-direction: column;
    line-height: 1;
  }

  .min-logo-name {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 0.95rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--color-brand-white);
  }

  .min-logo-suffix {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 0.46rem;
    text-transform: uppercase;
    letter-spacing: 0.3em;
    color: var(--color-brand-red);
    margin-top: 0.18rem;
  }

  .min-header-phone {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    line-height: 1.2;
    text-decoration: none;
  }

  .min-header-phone-label {
    font-family: var(--font-heading);
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.5);
  }

  .min-header-phone-number {
    font-family: var(--font-heading);
    font-size: 1rem;
    font-weight: 700;
    color: var(--color-brand-white);
  }

  @media (max-width: 480px) {
    .min-header-phone-number { font-size: 0.9rem; }
    .min-logo-name { font-size: 0.85rem; }
    .min-logo-suffix { font-size: 0.42rem; letter-spacing: 0.26em; }
  }
</style>
